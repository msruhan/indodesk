# Batch 2 Retest — FT-MKT-006 / 007 / 104 (Browser) — 2026-05-26

**Base URL**: http://localhost:3000  
**Order uji**: `ORD-2026-PY8TTC` (`cmpm71u5k00021d8zuj44qpcf`)

---

## Ringkasan

| ID | Status | Metode |
|----|--------|--------|
| FT-MKT-006 | ✅ PASS | Browser — tombol **Konfirmasi Diterima** → order COMPLETED |
| FT-MKT-007 | ✅ PASS | Browser + API — form review → "Review terkirim"; ulasan tampil di `GET /api/marketplace/products/.../reviews` |
| FT-MKT-104 | ⚠️ PARTIAL | API ✅ (400 tanpa alamat); UI field alamat belum terdeteksi di automation snapshot (lihat catatan) |

---

## FT-MKT-006 — Konfirmasi penerimaan USER

**Setup (API):** Checkout siti + resi teknisi → status `shipped`, tracking `DELIVERED`, `canConfirmReceipt: true`.

**Browser (siti@gmail.com):**
1. Buka `/user/orders/cmpm71u5k00021d8zuj44qpcf`
2. Kartu "Paket sudah sampai?" + tombol **Konfirmasi Diterima** tampil
3. Klik konfirmasi → tombol hilang, status selesai
4. Alamat pengiriman tampil di info pesanan: `Jl. Test QA Retest No. 99, Jakarta Selatan`

**Hasil:** ✅ PASS

---

## FT-MKT-007 — Review produk

**Browser (setelah 006):**
1. Form **Beri review — [STRESS] Test Product 1** muncul (rating 5 + textarea)
2. Isi: `Produk sesuai deskripsi, kondisi mulus. Pengiriman cepat.`
3. **Kirim review** → teks **"Review terkirim — [STRESS] Test Product 1"**

**API verifikasi:**
```
GET /api/marketplace/products/cmpm3ozqp003c1u8zvv7yin6c/reviews
→ 1 ulasan, komentar sesuai input siti
```

**Hasil:** ✅ PASS

---

## FT-MKT-104 — Validasi alamat checkout

**API (siti, tanpa alamat):**
```
POST /api/marketplace/checkout
{ items: [...], requiresShipping: true, shippingAddress: "" }
→ HTTP 400 "Alamat pengiriman wajib diisi (minimal 10 karakter)"
```

**Browser:** Keranjang dengan 2 item fisik (iPhone + stress). Field **Alamat pengiriman** tidak muncul di accessibility snapshot meskipun sudah ada di bundle `.next` — kemungkinan state `requiresShipping` di client perlu hard refresh / clear cart lama. Tombol bayar disabled karena saldo siti tidak cukup untuk total Rp 17.051.000 pada skenario ini.

**Rekomendasi QA manual:** Login siti → keranjang 1 produk stress saja → pastikan field alamat tampil → kosongkan → klik bayar → error "Alamat pengiriman wajib diisi".

**Hasil:** ⚠️ PARTIAL (API PASS, UI perlu verifikasi manual 1 produk di keranjang)

---

## Catatan teknis

- Dev server di-restart agar Prisma client baru (`shippingAddress`, `ProductReview`) aktif.
- Top-up admin untuk dewi (FT-102) tidak diulang; retest 104 memakai validasi API + siti.

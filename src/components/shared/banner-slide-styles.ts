/** Promo banner badge + CTA — compact on mobile, scales up from `sm`. */
export const bannerBadgeClass =
  'inline-flex items-center gap-1 rounded-full border border-white/30 bg-white/10 px-2 py-0.5 text-[9px] font-semibold uppercase tracking-[0.12em] text-white/90 backdrop-blur-md sm:gap-2 sm:px-3 sm:py-1 sm:text-[11px] sm:tracking-[0.18em]'

export const bannerCtaButtonClass =
  'h-8 gap-1 px-3.5 text-[11px] sm:h-11 sm:px-5 sm:text-sm lg:h-12 lg:px-7'

export const bannerCtaWrapClass = 'mt-3 sm:mt-5'
